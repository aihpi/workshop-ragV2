# Environment variables for RAG backend

# API Settings
API_V1_STR=/api/v1
PROJECT_NAME=RAG Backend

# CORS Settings (comma-separated)
BACKEND_CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Qdrant Settings
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_COLLECTION=documents

# =============================================================================
# PROVIDER SELECTION
# =============================================================================
# Choose your LLM and embedding providers:
#   LLM_PROVIDER: "ollama" (local) or "openai" (API-based)
#   EMBEDDING_PROVIDER: "local" (SentenceTransformers) or "openai" (API-based)
# =============================================================================
LLM_PROVIDER=ollama
EMBEDDING_PROVIDER=local

# =============================================================================
# LOCAL PROVIDERS (Ollama + SentenceTransformers)
# =============================================================================
# These settings are used when LLM_PROVIDER=ollama and EMBEDDING_PROVIDER=local

# Embedding Settings (local SentenceTransformers)
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
EMBEDDING_DIM=384

# Ollama Settings
OLLAMA_HOST=localhost
OLLAMA_PORT=11434
OLLAMA_MODEL=qwen2.5:7b-instruct

# =============================================================================
# OPENAI-COMPATIBLE API PROVIDERS
# =============================================================================
# These settings are used when LLM_PROVIDER=openai or EMBEDDING_PROVIDER=openai
# 
# To use the OpenAI-compatible API:
# 1. Copy this file to .env
# 2. Set your API key (NEVER commit your actual key to version control!)
# 3. Set LLM_PROVIDER=openai and/or EMBEDDING_PROVIDER=openai
# =============================================================================

# Your API key (required for OpenAI provider)
# Get your key from your API provider and set it here
OPENAI_API_KEY=your_api_key_here

# API base URL
OPENAI_BASE_URL=http://10.127.129.0:4000

# Model names
OPENAI_LLM_MODEL=gpt-oss-120b
OPENAI_EMBEDDING_MODEL=octen-embedding-8b
OPENAI_EMBEDDING_DIM=4096

# =============================================================================
# COMMON LLM SETTINGS (apply to both providers)
# =============================================================================
LLM_MAX_TOKENS=512
LLM_TEMPERATURE=0.7
LLM_TOP_P=0.9
LLM_TOP_K=40

# Document Processing Settings
CHUNK_SIZE=512
CHUNK_OVERLAP=128
DATA_FOLDER=../data

# Chat History Settings
CHAT_HISTORY_FOLDER=../chat_history
MAX_CHAT_HISTORY=10
